export default function Page() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Dynasty DNA — Rewrite</h1>
      <p>Next.js app scaffolded from the PRD. Health check below:</p>
      <pre><a href="/api/health">/api/health</a></pre>
    </main>
  );
}

